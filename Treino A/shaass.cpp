#include <bits/stdc++.h>
using namespace std;

int solve() {

}

int main () {

    int n, m;
    cin >> n;
    vector<int> list(n);
    for (int i = 0; i < n; i++)
        cin >> list[i];
    cin >> m;
    int matriz[m][m] = {0};
    for (int i = 0; i < m; i++)
        
    


    return 0;
}